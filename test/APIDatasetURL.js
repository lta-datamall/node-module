import assert from 'chai';
import { APICall as APICall } from '../dist/lib/APICall';

describe('APICall', _ => {
    it('should return a Datamall2 response', _ => {
        assert.equal(API)
    })
})